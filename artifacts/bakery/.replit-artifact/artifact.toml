kind = "web"
previewPath = "/"
title = "New Model Bread"
version = "1.0.0"
id = "artifacts/bakery"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = []
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/bakery run dev"

[services.env]
PORT = "3000"
BASE_PATH = "/"
